import React, { Component } from 'react';
import { connect } from 'react-redux';

//want to map through pages data, and retrieve page based on slug

function PageB(props) {
    return (
        <div>
          <p>This is hardcoded page B</p>
        </div>
    );
}

export default PageB;
